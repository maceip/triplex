<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        LiquidGlassTheme follows isSystemInDarkTheme(), so the platform theme has
        to follow the system too or the pre-Compose frame flashes the wrong way.
    -->
    <style name="Theme.Triplex" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
